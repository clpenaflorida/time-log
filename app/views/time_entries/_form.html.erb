<%= form_for(@time_entry) do |f| %>
  <% if @time_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_entry.errors.count, "error") %> prohibited this time_entry from being saved:</h2>
 
      <ul>
      <% @time_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<form role="form">
<div class="row">
<div class="col-md-6">
  <div class="form-group">
    <label>Name: </label> <%= f.select(:name, options_for_select({ "Monday" => "Monday", "Tuesday" => "Tuesday", "Wednesday" => "Wendesday", "Thursday" => "Thursday", "Friday" => "Friday", "Saturday" => "Saturday"}, "Monday"), {}, {:class => "selectpicker show-tick"}) %>
  </div>
 </div>
</div>
  <div class="field">
    <label>Time in: </label>
    <%= f.time_select :starts_at %>
  </div>
  </p>
  
  <div class="field">
    <label>Time out: </label>
    <%= f.time_select :finish_at %>
  </div>
 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</form>
<script>
document.getElementById("demo").innerHTML = "My First JavaScript";
</script>